let Member;_189‍.x([["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["update",()=>update]]);_189‍.w("../../models/member",[["default",["Member"],function(v){Member=v}]]);

       const write = async ctx => {
  const { id, password, userName, phoneNumber, address, email } =
    ctx.request.body;
  const member = new Member({
    id,
    password,
    userName,
    phoneNumber,
    address,
    email
  });
  try {
    await member.save();
    ctx.body = member;
  } catch (e) {
    ctx.throw(500, e);
  }
};

       const list = (ctx) => {
    ctx.body="실험중";
};

       const read = (ctx) => {};

       const remove = (ctx) => {};

       const update = (ctx) => {};
